import { PagePlaceholder } from "../../components/PagePlaceholder";

export function OverviewPage() {
  return (
    <PagePlaceholder
      title="Oversigt"
      description="Her kommer stats: antal træninger, minutter, cardio og kropsvægt-graf med tidsrums-toggle."
    />
  );
}
